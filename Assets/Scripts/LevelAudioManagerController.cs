using UnityEngine;

public class LevelAudioManagerController : MonoBehaviour
{
    [SerializeField] private AudioSource bgmAudioSource;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
